import { generateDashboardMetadata } from "@/lib/metadata";

export const LOGIN_PAGE_METADATA = generateDashboardMetadata({
  title: "Login - Estatein",
  description: "Log in to access Estatein dashboard.",
});
