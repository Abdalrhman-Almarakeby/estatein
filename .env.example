# ────────────────────────────────────────────────────────────────
# 🌍 Application Configuration
# ────────────────────────────────────────────────────────────────

# The public-facing base URL of your Next.js application (must include protocol)
NEXT_PUBLIC_BASE_URL="https://example.com"

# The Vercel deployment URL of your application (auto-assigned by Vercel)
NEXT_PUBLIC_VERCEL_URL="example.vercel.app"

# Current environment mode (options: "development" | "production")
NODE_ENV="development"

# ────────────────────────────────────────────────────────────────
# 🔐 Authentication Configuration
# ────────────────────────────────────────────────────────────────

# Base URL for authentication callbacks
NEXTAUTH_URL="https://example.com"

# Secret key for NextAuth.js (must be a secure 32-character string)
NEXTAUTH_SECRET="your-secure-nextauth-secret"

# Admin email with signup authority and default admin privileges
ADMIN_EMAIL="admin@example.com"

# ────────────────────────────────────────────────────────────────
# ✅ reCAPTCHA Configuration
# ────────────────────────────────────────────────────────────────

# Public site key for Google reCAPTCHA v3
NEXT_PUBLIC_RECAPTCHA_SITE_KEY="your-recaptcha-site-key"

# Secret key for server-side reCAPTCHA verification
RECAPTCHA_SECRET_KEY="your-recaptcha-secret-key"

# ────────────────────────────────────────────────────────────────
# 📧 Email Configuration
# ────────────────────────────────────────────────────────────────

# Gmail address used for sending emails
GMAIL_EMAIL="your-email@gmail.com"

# Gmail App Password (generated in Google Security settings)
GMAIL_APP_PASSWORD="your-gmail-app-password"

# ────────────────────────────────────────────────────────────────
# 🗄️ Database Configuration (PostgreSQL)
# ────────────────────────────────────────────────────────────────

# Main PostgreSQL connection string (replace with actual credentials)
POSTGRES_URL="postgres://user:password@host:port/database"

# Prisma-specific connection string
POSTGRES_PRISMA_URL="postgresql://user:password@host:port/database?schema=public"

# Connection string for local development without SSL
POSTGRES_URL_NO_SSL="postgres://user:password@host:port/database?sslmode=disable"

# Connection string for environments without connection pooling
POSTGRES_URL_NON_POOLING="postgres://user:password@host:port/database?pooling=false"

# Individual database credentials (for advanced configurations)
POSTGRES_USER="your-db-username"
POSTGRES_HOST="your-db-host"
POSTGRES_PASSWORD="your-db-password"
POSTGRES_DATABASE="your-db-name"

# ────────────────────────────────────────────────────────────────
# 🔑 Key-Value Storage Configuration
# ────────────────────────────────────────────────────────────────

# Base URL for the Key-Value store
KV_URL="your-kv-url"

# REST API endpoint for Key-Value store
KV_REST_API_URL="your-kv-rest-api-url"

# Authentication token for full access to Key-Value storage
KV_REST_API_TOKEN="your-kv-rest-api-token"

# Read-only authentication token (for restricted access)
KV_REST_API_READ_ONLY_TOKEN="your-kv-read-only-token"

# ────────────────────────────────────────────────────────────────
# 🚀 Redis Configuration (Upstash)
# ────────────────────────────────────────────────────────────────

# Upstash Redis REST API endpoint
UPSTASH_REDIS_REST_URL="your-upstash-redis-url"

# Authentication token for Upstash Redis
UPSTASH_REDIS_REST_TOKEN="your-upstash-redis-token"

# ────────────────────────────────────────────────────────────────
# 🗂️ Storage Configuration (Blob Storage)
# ────────────────────────────────────────────────────────────────

# Authentication token for read/write access to blob storage
BLOB_READ_WRITE_TOKEN="your-blob-read-write-token"
